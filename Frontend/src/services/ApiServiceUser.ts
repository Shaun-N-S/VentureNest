import axios from "axios";

interface signup {
  name: string;
  email: string;
  password: string;
  confirmPassword: string;
}

export const userSignup = async (values: signup) => {
  try {
    const response = axio;
  } catch (error) {}
};
